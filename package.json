{
  "name": "project-vimlet-meta",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format2":  "node_modules/.bin/eslint src/**/*.js --fix",
    "format": "node_modules/.bin/prettier **/*.js --write",
    "build": "node build/build.js"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "pre-commit": "^1.2.2",
    "prettier": "^1.7.3",
    "eslint": "^4.8.0"
  },
  "pre-commit": [
    "format",
    "build"
  ],  
  "eslintConfig": {
    "rules": {
      "quotes": [2, "double"]
    }
  },
  "eslintIgnore": [
    "**/node_modules/**"
  ]
}
